# Deploy workflow removed — only test workflows are active.
# This file is kept as a placeholder to prevent GitHub from using a cached version.
on:
  workflow_dispatch:
    inputs:
      placeholder:
        description: 'This workflow is disabled'
        required: false
